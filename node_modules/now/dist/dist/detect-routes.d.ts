import { Route } from '@now/routing-utils';
import { Builder } from './types';
interface RoutesResult {
    defaultRoutes: Route[] | null;
    redirectRoutes: Route[] | null;
    error: {
        [key: string]: string;
    } | null;
}
export declare function detectOutputDirectory(builders: Builder[]): string | null;
export declare function detectApiDirectory(builders: Builder[]): string | null;
export declare function detectApiExtensions(builders: Builder[]): Set<string>;
export declare function detectRoutes(files: string[], builders: Builder[], featHandleMiss?: boolean, cleanUrls?: boolean, trailingSlash?: boolean): Promise<RoutesResult>;
export {};
