import { Route, Builder } from './types';
interface RoutesResult {
    defaultRoutes: Route[] | null;
    error: {
        [key: string]: string;
    } | null;
}
export declare function detectRoutes(files: string[], builders: Builder[]): Promise<RoutesResult>;
export {};
