import { NodeVersion } from '../types';
export declare const defaultSelection: NodeVersion;
export declare function getSupportedNodeVersion(engineRange?: string, silent?: boolean): Promise<NodeVersion>;
